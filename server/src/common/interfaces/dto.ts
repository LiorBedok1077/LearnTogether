// Global dto interfaces & types

// list actions enum - used whenever adding/removing items in a list
export enum listActionsEnum { invite, remove, modify }

// like / unlike options
export enum likeOrDislikeEnum { like, dislike }